@page "/"

@using Starbender.RecipeApp.Services.Contracts
@using Starbender.RecipeApp.Services.Contracts.Dtos
@using Starbender.RecipeApp.Blazor.Components

@inject IRecipeAppService RecipeService
@inject NavigationManager NavigationManager

<MudContainer MaxWidth="MaxWidth.ExtraLarge" Class="py-2 w-100">

    @if (_isLoading)
    {
        <MudProgressCircular Indeterminate="true" Color="Color.Primary" />
    }
    else
    {
        <RecipeTable />
    }

</MudContainer>

@code {

    private bool _isLoading = true;

    protected override async Task OnParametersSetAsync()
    {
        _isLoading = false;
    }

    private async Task HandleDialogSavedAsync(RecipeDto _)
    {
        //StateHasChanged();
    }
}
